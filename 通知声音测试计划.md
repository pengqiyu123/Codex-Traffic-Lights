# Codex 通知与声音测试计划

本计划只验证通知和声音，不重新验收灯光动画。灯光状态继续以 `测试计划.md` 为准。

## 事件矩阵

| # | 事件 | 触发条件 | 期望托盘通知 | 期望声音 | 当前判定 | 测试方式 |
|---|---|---|---|---|---|---|
| 1 | `COMPLETED` 任务完成 | `WORKING` / `WAITING_APPROVAL` / `WAITING_USER_INPUT` 转为 `IDLE` | `项目名：任务已完成` | Windows `MB_OK` 完成音 | 待测 | 在 VSCode Codex 启动一个 5 秒以上任务，等任务完成回到绿灯 |
| 2 | `WAITING_APPROVAL` 待审批确认 | 进入 `WAITING_APPROVAL` | `项目名：待审批确认` | Windows `MB_ICONEXCLAMATION` 提示音 | 待测 | 触发 sandbox/权限批准请求，停在批准弹窗 |
| 3 | `WAITING_USER_INPUT` 计划模式需要输入 | 进入 `WAITING_USER_INPUT` | `项目名：计划模式需要输入` | Windows `MB_ICONQUESTION` 问题音 | 待测 | 切换计划模式，触发提问机制并停在提问界面 |
| 4 | `ERROR` 运行异常 | 进入 `ERROR` | `项目名：运行异常` | Windows `MB_ICONHAND` 错误音 | 待测 | 临时改错 VSCode Codex 配置或触发真实失败样本 |

## 不应触发通知/声音

| 场景 | 期望 |
|---|---|
| 程序刚启动时已经存在历史 `IDLE` / `WORKING` / `WAITING_*` / `ERROR` 状态 | 不弹通知，不响 |
| `IDLE -> WORKING` | 不弹通知，不响 |
| `任意状态 -> OFFLINE` | 默认不弹通知，不响 |
| `ERROR -> IDLE` | 不弹“任务已完成”，不响完成音 |
| 同一个项目持续停在同一个提醒事件状态 | 不重复弹通知，不重复响 |
| ClaudeCode 会话进入 `ERROR` / 等待态 | 不影响 Codex 通知和声音 |

## 开关测试

| # | 通知按钮 | 静音按钮 | 触发事件 | 期望 |
|---|---|---|---|---|
| 1 | 开 | 关 | 任意 4 类事件 | 有通知，有声音 |
| 2 | 关 | 关 | 任意 4 类事件 | 无通知，有声音 |
| 3 | 开 | 开 | 任意 4 类事件 | 有通知，无声音 |
| 4 | 关 | 开 | 任意 4 类事件 | 无通知，无声音 |

## 去重与重入测试

1. 进入 `WAITING_USER_INPUT` 后停住 20 秒：只应提醒一次。
2. 从 `WAITING_USER_INPUT` 回到 `WORKING`，再进入 `WAITING_USER_INPUT`：应再次提醒。
3. 进入 `WAITING_APPROVAL` 后不点批准停住：只应提醒一次。
4. 批准后再次触发新的审批请求：应再次提醒。
5. 任务完成提醒只应在真实回到 `IDLE` 时出现一次。

## 记录表

| 测试项 | 结果 | 备注 |
|---|---|---|
| 1. 完成通知 + 完成音 |  |  |
| 2. 待审批通知 + 提示音 |  |  |
| 3. 计划提问通知 + 问题音 |  |  |
| 4. 运行异常通知 + 错误音 |  |  |
| 5. 初始快照不提醒 |  |  |
| 6. `IDLE -> WORKING` 不提醒 |  |  |
| 7. `ERROR -> IDLE` 不误报完成 |  |  |
| 8. 通知按钮关闭只屏蔽通知 |  |  |
| 9. 静音按钮开启只屏蔽声音 |  |  |
| 10. 同状态去重 |  |  |
| 11. 离开后重入再次提醒 |  |  |
| 12. ClaudeCode 会话不触发 Codex 提醒 |  |  |

## 测试口径

- 只记录真实 VSCode Codex 触发结果，不用 mock 或示例数据当作通过。
- 通知文案必须和表格一致；项目名可以是实际显示名。
- Windows 系统音受系统音量、主题和设备影响，验收重点是“有无声音”和“4 类事件音色可区分”。
- 如果某类事件无法稳定触发，记录为“未触发/待复测”，不要写通过。
