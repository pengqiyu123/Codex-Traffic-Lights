# 已废弃：原始假设 Prompt

本文件仅保留为项目早期提示词来源记录，不再作为开发依据。Codex Traffic Lights 的实现以 `docs/Codex-State-Audit.md` 和 `docs/PRD.md` 中确认的 6 个产品状态为准。

# 修订Codex红绿灯组件完整版开发提示词（已新增弹窗确认/等待审批/等待审阅三大交互状态，全7种Codex原生工作状态，可直接发给AI编程）
```
你现在是Windows桌面悬浮小组件全栈开发工程师+UI前端设计师，开发【Codex工作状态桌面悬浮指示灯组件】，严格按照附图竖版黑底红绿灯样式，结合Codex CLI官方7类原生运行状态（含弹窗审批、等待审阅、等待输入交互状态）定制三色灯特效规则，分UI规范、7态灯光逻辑、交互功能、开发方案四部分，最终输出完整可运行源码+部署教程。

### 一、UI视觉规范（完全参照用户提供原图）
1. 组件外形：竖向圆角长条形悬浮窗体，哑光深黑底色，永久置顶桌面顶层不被遮挡；
2. 顶部：蓝紫渐变云朵图标内含>_终端符号，图标下方白色大写「CODEX」字样；
3. 中部指示灯从上到下固定顺序：红灯、黄灯、绿灯三个圆形指示灯；
4. 底部：黄色中文状态文字，跟随实时工作状态自动切换；
5. 右侧竖排6枚半透明悬浮按钮（从上→下）：消息铃铛、缩小-、放大+、菜单三点、电源启停、静音；鼠标悬浮按钮高亮显实色，常态低透明度。

### 二、Codex 7种官方运行状态+对应三色灯发光逻辑（新增弹窗确认/审批/审阅状态，依据Codex CLI审批机制&Agent生命周期）
1.【离线休眠｜红灯常亮，黄、绿灯全熄灭】
进程未启动/后台关闭、服务未挂载；底部文字：待机离线
2.【深度高负载运算｜黄灯高频快速呼吸（粗暖黄光环，1秒完整呼吸周期），红绿熄灭】
高负荷代码重构、全项目批量修改、长耗时复杂推理（xhigh/high负载）；底部：深度工作中
3.【常规轻量运行｜黄灯慢速柔和呼吸（细黄光环，3秒一呼吸循环），红绿熄灭】
普通代码补全、单文件编辑、简易指令执行（low/medium负载）；底部：常规运行中
4.【任务排队积压｜黄灯低频间歇闪烁（0.5s亮0.5s灭，微弱浅光晕），红绿熄灭】
任务队列堆积，新指令排队等候执行；底部：任务排队等候
5.【弹窗等待审批（核心新增：跳出权限确认框）｜黄灯+绿灯慢速交替小幅度闪烁，红灯灭】
触发高危操作、跨目录修改、外网请求、系统UAC权限弹窗，Codex暂停等待用户手动点确认/拒绝（approval弹窗触发）；底部：待用户审批确认
6.【等待审阅变更｜绿灯缓慢呼吸发光无外圈强光，红黄灯熄灭】
Codex生成代码改动Diff、等待用户审阅代码变更、等待补充输入指令（ready for review）；底部：待审阅代码变更
7.【空闲就绪待命｜绿灯常亮无任何光晕，红黄全灭】
后台常驻就绪、无运行任务，等待下发新指令；底部：空闲待命
8.【进程异常卡死｜红灯+黄灯快速交替频闪，绿灯熄灭】
程序崩溃、命令报错卡死、沙盒运行失败；底部：进程异常报错

### 三、桌面组件交互功能
1. 置顶悬浮：窗口永久在桌面最上层；
2. 拖拽贴边：按住组件自由拖动，贴近桌面左右侧边自动半隐藏缩进，鼠标移入边缘自动滑出；
3. 右侧按钮定义：
铃铛=审批弹窗/任务完成系统通知开关；±=整体缩放组件大小；三点=设置面板（自定义呼吸速度、灯光配色）；电源=一键启动/关闭本地Codex后台进程；静音=开关状态切换提示音；
4. 附加配置：支持开机自启、自定义光晕亮度与颜色。

### 四、技术实现双方案
方案1：Python+PyQt5（首选，可读取本地Codex进程状态，对接CLI状态接口实时同步灯效）
方案2：AutoHotkey V2（轻量化，单exe免依赖直接运行）
输出：完整源码 + 分步安装部署教程 + 自定义灯效修改说明
```

## 精简速发版Prompt（短句快速提问用）
```
开发Windows桌面Codex悬浮指示灯挂件，参考附图黑竖条三灯样式；
共8种状态灯效：红灯常亮=离线；黄灯快慢呼吸分深度/常规工作，黄灯闪烁=排队；黄灯+绿灯交替闪=弹出确认框待审批；绿灯呼吸=待审阅代码，绿灯常亮=空闲；红黄灯频闪=异常；支持置顶拖拽贴边隐藏、侧边快捷按钮，输出PyQt或AHK完整源码+部署教程。
```

### 状态来源备注
1. 弹窗审批状态来自Codex默认on-request审批策略，高危操作自动弹出确认弹窗、任务暂停等待用户确认，是高频实用状态；
2. 待审阅Diff是Codex Agent编辑后生成变更预览、等待用户审核放行的专属交互状态，海外同款Codex桌面挂件标配该状态指示灯。
