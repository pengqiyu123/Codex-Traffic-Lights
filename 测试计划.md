# Codex 状态灯测试计划

本计划只验证 Codex 状态灯。Expanded 面板和全局聚合都应过滤 ClaudeCode 会话；Claude hook 可以继续写入 registry，但不能出现在 UI，也不能抢占 Codex 全局状态。

## 状态矩阵

| # | 状态 | 期望灯光 | 当前判定 | 测试方式 |
|---|---|---|---|---|
| 1 | `WORKING` 正在工作 | 黄灯慢呼吸 | 已验证 | 在 VSCode Codex 中启动一个 5 秒以上任务，观察黄灯 |
| 2 | `IDLE` 空闲待命 | 绿灯常亮 | 已验证 | 等 VSCode Codex 任务完成，观察是否回绿灯 |
| 3 | `WAITING_APPROVAL` 待审批确认 | 黄灯 + 绿灯交替慢闪 | 已验证 | 触发 sandbox/权限批准请求，停在批准弹窗，观察黄绿交替慢闪 |
| 4 | `WAITING_USER_INPUT` 待用户输入 | 黄灯 700ms 间歇闪 | 已验证，复测通过 | 切换到计划模式触发提问机制，黄灯能亮到峰值，且不会被 IPC 高频刷新打断 |
| 5 | `ERROR` 运行异常 | 红灯 + 黄灯快闪 | 已验证 | 临时改错 VSCode Codex 配置触发真实失败样本；只在 IPC 出现 `failed` 或 `systemError` 时判定通过 |
| 6 | `OFFLINE` 离线休眠 | 总灯红灯常亮；Expanded 已关闭项目红灯慢闪 3 秒后隐藏 | 已验证 | 关闭 VSCode/Codex 插件、归档/删除 Codex thread，或关闭某个 VSCode 窗口；确认无 active Codex session 后总灯回红灯，Expanded 中断开的项目约 1 秒一次红灯慢闪，3 秒后消失 |

## 回归测试

1. 长任务：启动超过 5 分钟的 VSCode Codex 任务，只要 IPC 仍是 `threadRuntimeStatus.type = active`，红绿灯必须保持 `WORKING`，不能按时间降级为 `IDLE`。
2. 旧 turn 残留：如果 `lastTurn.status = inProgress`，但 `threadRuntimeStatus.type = idle`，红绿灯应显示 `IDLE`。
3. 多会话：同时存在 VSCode Codex 和 ClaudeCode 时，Expanded 面板只显示 Codex 会话。
4. 聚合：Claude 会话即使是 `ERROR`，也不能影响 Codex 全局灯色。
5. 计划最终确认：计划模式完成后停在“是否实施此计划”，真实 IPC 已确认会出现 `planImplementation.isCompleted = false`，应映射为 `WAITING_APPROVAL`；实施/取消后 `isCompleted = true` 应清除等待态并回到真实 runtime 状态。

## 诊断规则

- 不用 `fileChange`、`commandExecution`、普通 `inProgress` 推断审批或用户输入等待。
- `WAITING_APPROVAL` 已通过 sandbox/权限批准请求实测；计划模式最终“是否实施此计划”的真实信号是 `planImplementation.isCompleted = false -> true`，产品已按该布尔信号识别，仍需停在确认界面做灯效复测。
- `WAITING_USER_INPUT` 已通过计划模式提问机制实测并复测通过；黄灯间歇闪周期调整为 700ms，并避免同状态 IPC 高频刷新重启动画。如需留档，可补采一条脱敏 IPC 样本。
- 计划最终确认态不能用普通 `completed` / `idle`、计划正文、prompt、turn 内容推断为审批；只允许 `threadGoalResumeConfirmation` 明确等待字段或 `planImplementation.isCompleted = false` 这类结构化信号映射为 `WAITING_APPROVAL`。
- `ERROR` 已通过临时错误配置触发真实异常场景验证；仍只接受真实 `failed` / `systemError` 信号，不用普通失败描述或长时间无响应推断。
- `OFFLINE` 只接受 VSCode/Codex 断开后无 Codex session 的状态；IPC 真实断线时必须清理当前 connector 管理的 `vscode-ipc::` sessions，并按剩余 Codex sessions 重新聚合。
- 单个 VSCode/Codex 项目关闭不一定会让整条 `codex-ipc` 管道断开；如果 IPC 收到 `client-status-changed: disconnected`，只清理该 client 来源的 sessions；如果收到 `thread-archived` / `thread/closed`，只清理对应 conversation。
- Expanded 中的断开项目退场提示是 UI-only：红灯约 1 秒一次慢闪，3 秒后隐藏；退场列不回写 registry、不参与全局聚合、不新增第 7 个产品状态。
- 真实上游数据不可用时，测试结论应写“不可观测/暂不可支持”，不要写“通过”。
